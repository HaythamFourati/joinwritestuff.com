{
  "name": "monthly-detox",
  "version": "1.1.0",
  "description": "Hug in the Mail — WordPress theme. Hand-authored plain-CSS design system (assets/design.css + design.js). No build step.",
  "scripts": {
    "sync": "browser-sync start -p 'your-site.local' --files '**/*.php' 'assets/design.css' 'assets/design.js'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "3.0.4"
  }
}
